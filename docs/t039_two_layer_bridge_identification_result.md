# T039 双层桥接模型辨识与独立验证结果

## 判定

正式状态为`passed`，8项冻结约束全部通过。拟合入口首先确认T038准入工作流通过、决策为`provisional_ready_for_identification`，并验证准入报告记录的数据SHA-256与当前输入一致；随后使用6个辨识循环拟合，3个验证循环未参与参数估计。

## 预测结果

| 深度 | 单步RMSE（m³/m³） | 单步技能 | 整循环RMSE（m³/m³） | 整循环技能 | 整循环偏差（m³/m³） |
|---|---:|---:|---:|---:|---:|
| 10 cm | 0.000672 | 0.672 | 0.001217 | 0.960 | +0.000356 |
| 25 cm | 0.000714 | 0.275 | 0.001353 | 0.936 | +0.000611 |

每个深度的验证均包含3个循环、288个状态转移。两层单步技能高于0.10，整循环技能高于0，整循环绝对偏差低于0.01 m³/m³，全部滚动预测位于0.02至0.65 m³/m³。

深层单步技能低于浅层，但整循环技能达到0.936。这是因为深层每步变化幅度较小，持久性基线本身较强；整循环基线固定为循环初值，能够更清楚地区分模型对累计变化的解释能力。后续控制评价必须继续分别报告两层状态，不能由加权指标隐藏深层单步响应的不确定性。

## 冻结参数

浅层状态增量参数依次为梯度−0.05773、实际灌水0.007620、太阳辐射尺度项−0.001188、VPD项−0.0001154、截距−0.00006358。深层对应参数为0.04018、0.0004413、−0.0002631、−0.00006780和近零截距。参数均满足冻结方向边界，两个有界求解均收敛。

该参数集来自独立非线性合成对象生成的数据，不是虚拟对象真值的复制。下一阶段MPC只能读取本结果中的冻结参数及其哈希，不得访问对象参数。

## 复现与边界

协议与代码在首次运行前由提交`74bbd01`冻结。结果JSON和288行验证预测连续两次运行的SHA-256分别为`a6040a1df9e3760c364c67a2397e8868063bca5a6c074fe4bf5da71eaaaa282f`和`b4818a5bec1835716602e7cd4683962b57ecd6e6e1c49f57856df3dea53d7ed3`。

本结果支持独立合成双层对象上的模型交接，不支持红壤实测参数或现场预测性能。双层闭环锁定评价尚未完成，R027继续保持缺失。本任务未生成图。
