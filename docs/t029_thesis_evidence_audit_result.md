# T029 毕业设计证据审计结果

## 审计结论

仓库证据审计状态为`passed`，19项一致性检查全部通过。当前结果证明任务、证据文件、冻结状态、配置版本、论文关键数字以及正文数字之间能够自动回溯；它不改变红壤实测与实体试验仍然缺失的事实。

## 审计范围

| 对象 | 数量 | 结果 |
|---|---:|---|
| 任务台账 | 38项 | 编号连续、状态合法、完成项具有日期与证据 |
| 毕业设计需求 | 29项 | 路径与预期状态一致 |
| 核心结果状态 | 16项 | 全部匹配冻结分类 |
| 配置哈希绑定 | 8项 | 全部与当前配置一致 |
| 结果代码提交 | 8项 | Git历史中全部存在 |
| 论文关键数字 | 15项 | 全部匹配JSON来源 |
| 第3—4章正文数字绑定 | 16项 | 文本与冻结JSON同时匹配 |
| 支持主张 | 6项 | 证据状态满足要求 |
| 条件主张 | 4项 | 继续保持条件状态 |
| 禁止主张 | 5项 | 未被自动升级 |

失败和停止结果被正确保留。R005、R006、R007和R015分别对应可信监督失败、停止以及原可辨识性判据失败，仍能支持失效边界分析，但不能转写为算法成功。

## 当前可写主张

1. 已实现任务、命令、反馈、联锁和版本追溯的软件在环平台原型；
2. 基础MPC在冻结仿真中形成少灌水伴轻微亏缺增加的权衡；
3. 幅值敏感信息量可在已知结构合成条件下区分采样设计等级；
4. 可信监督存在恢复死锁和统一安全预算冲突等可复核失效边界。
5. 动态误差分解入口能够在标定误差地板高于独立验证残差时拒绝输出伪过程噪声参数。
6. 独立双层非线性对象生成的数据通过结构质量和信息准入，可以进入模型拟合。

## 当前条件主张

- 红壤实测模型参数：缺少R003；
- 真实传感器与灌水反馈回放：缺少R003和R009；
- 真实节水率、产量或品质效果：缺少R003、R009和R010。
- 双层辨识模型直接驱动MPC的锁定闭环评价：缺少R027。

在对应需求完成前，以上内容只能写成后续工作或验证目标。

## 当前禁止主张

- MPC在所有条件下全面优于强规则控制；
- 当前可信监督已经保证安全并优于基础MPC；
- 本机MQTT结果证明现场网络和泵阀满足实时可靠性；
- 三个湿干循环普遍足以辨识红壤根区模型。
- T010单层MPC结果验证了T024双层辨识模型的控制效果。

## 毕业设计成熟度

`simulation_software_thesis_requirements`为`ready`，说明“智慧管控平台＋MPC仿真适用性与失效边界”版本已经具备主要证据。

`red_soil_model_requirements`为`blocked`，阻断项是R003和R009。`entity_effect_requirements`为`blocked`，还增加R010。因此当前论文题名和摘要必须保留“适用性研究”或“面向红壤区”的范围限定。

`integrated_two_layer_mpc_requirements`为`blocked`，阻断项是R027。T036接口审计和T037验证协议已经完成，但在新的双层闭环锁定评价完成前，第3章双层模型与第4章单层MPC应作为互补证据陈述。

## 运行方式

```bash
python scripts/audit_thesis_evidence.py
```

结构化结果位于`data/processed/thesis_evidence_audit_v1.json`。本任务没有生成图形。
