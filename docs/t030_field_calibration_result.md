# T030 现场水分传感器与流量计标定入口结果

## 判定

T030在提交`9a1b070`冻结模板、配置、计算程序和合成接入样例。随后提交`4a7b1ba`修正失败数据使用严格JSON `null`返回的格式问题，未改变阈值、样例或通过指标；正式记录结果绑定该修正提交。工作流状态为`passed`，水分与流量标定质量检查均通过，且程序没有从静态标定中推断过程噪声或提前启用现场信息量阈值。

## 合成水分标定结果

| 传感器 | 深度 | 水分等级×重复 | 参考跨度（m³/m³） | 校正RMSE（m³/m³） | 留一等级RMSE（m³/m³） | 残差SD（m³/m³） |
|---|---|---:|---:|---:|---:|---:|
| S10 | 10 cm | 5×3 | 0.24 | 0.002375 | 0.002377 | 0.002551 |
| S25 | 25 cm | 5×3 | 0.24 | 0.002546 | 0.002549 | 0.002735 |

两只传感器的线性斜率分别为0.9692和1.0391。程序采用两者中较大的校正残差标准差0.002735 m³/m³，作为交给模型准入模块的保守测量噪声候选。

该数值由人为构造的合成误差产生，不代表实际探头精度。正式试验必须替换为传感器与红壤样品的重复标定记录。

## 合成流量标定结果

FLOW_01覆盖10、20和30 L三个参考体积，每点3次重复。拟合斜率为0.9799，校正相对RMSE为0.00539，最大绝对相对误差为0.00990，留一运行点相对RMSE为0.00539。

流量误差属于灌水输入不确定性，应在后续模型辨识和控制回放中单独传播，不能当作水分传感器噪声。

## 模型准入交接状态

当前交接状态为`measurement_noise_ready_process_noise_pending`：

- 水分测量噪声候选已能从标定残差计算；
- 流量计校正参数和输入误差已能计算；
- 根区模型过程噪声仍为空；
- 现场信息量阈值保持未就绪。

下一步必须使用动态湿干循环，在模型结构和参数拟合规则冻结后，通过独立循环残差分离测量噪声、过程噪声与输入误差。只有该步骤完成后，才能建立替代T027合成参考的现场准入配置。

## 产物

- 水分空模板：`data/templates/moisture_sensor_calibration_template.csv`
- 流量空模板：`data/templates/flow_meter_calibration_template.csv`
- 配置：`configs/field_calibration_quality_v1.yaml`
- 运行入口：`scripts/assess_field_calibration.py`
- 结构化结果：`data/processed/field_calibration_example_v1.json`

本任务没有生成图形。结果不改变R003、R009和R010的缺失状态。
