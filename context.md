# Context

## 项目目标

- 研究对象为设施大棚单分区根区水分控制。
- 当前主线为“独立虚拟对象 + 强规则基线 + 区间 MPC + 后续可信回退”。
- 平台最终负责参数版本、任务、命令、联锁、反馈和试验追溯，当前先验证控制算法基础。

## 当前状态

- GitHub唯一远端为 `https://github.com/gzxxxxxx178-ship-it/greenhouse_rootzone_mpc.git`，本地 `origin` 已配置并使用HTTPS认证。
- 当前开发分支为 `codex/bootstrap-rootzone-mpc`，已建立远端跟踪关系。
- 已建立分类目录、Python 项目和独立虚拟环境。
- 已实现非线性根区水量平衡虚拟对象，包含灌溉效率、蒸散胁迫响应和田间持水量以上的非线性排水。
- 已实现带最小开关保持逻辑的阈值脉冲规则基线。
- 已实现离散候选序列区间 MPC。MPC 使用简化的线性内部模型，不读取虚拟对象真值参数。
- 已建立标称、高蒸散、低灌水效率和强排水四种烟雾场景及成对运行入口。
- 已导出逐步轨迹、汇总指标和比较图，并完成关键计算测试。
- 第一轮烟雾试验中，强规则在四种场景均只有2个低于目标区间的时间步；基础 MPC 为46至72个时间步，但这些越界主要贴近下限。MPC命令灌水总量比规则少6至12 mm，存在“灌水较少但轻微亏缺更多”的明确权衡。
- 当前结果说明链路可运行，同时暴露MPC水量惩罚、下限惩罚和动作离散化需要在开发集上审查。该结果不是算法优势证据，不允许直接调整后写成正式结论。
- T008已建立`scenario_design_v1`：48个开发场景和120个锁定评价场景，分别使用独立拉丁超立方设计与互不重叠的场景种子；三类蒸散日程在两个集合内均衡。
- 场景覆盖根深、田间持水量、萎蔫点、初始有效水分、灌水效率、排水、蒸散倍率、预测偏差及过程/传感噪声。范围只作为敏感性包络，不代表红壤统计分布。
- `docs/evaluation_contract.md`已冻结基础MPC的主要终点、统计对象、结果分类和禁止主张。锁定评价集不得用于T009参数选择。
- T009完成27组规则参数和12组MPC参数的有限网格标定。规则27/27组、MPC 11/12组满足开发约束，选择过程未读取锁定评价集。
- 已冻结RULE010：启动阈值0.205、停止阈值0.225、2 mm脉冲；开发集灌水命令中位数75 mm，亏缺积分中位数0，安全违反场景0。
- 已冻结MPC007：干旱惩罚32000、水量惩罚0.04、动作变化惩罚0.05；开发集灌水命令中位数70 mm，亏缺积分中位数0.00298，安全违反场景0。
- 开发结果暂表现为MPC节水伴轻微亏缺的权衡；该方向是否成立必须由T010锁定确认性评价决定。
- T010已完成120个锁定场景、240次方法运行和20000次场景级bootstrap。预设分类为`WATER_SAVING_WITH_DEFICIT_TRADEOFF`，仅表示仿真灌水命令减少伴亏缺代价。
- MPC相对规则的灌水命令配对中位差为−6 mm，95%区间[−8, −4] mm，97.5%的场景减少、2.5%持平、无增加；亏缺积分配对中位差为+0.000680，95%区间[+0.00000215, +0.001684]，59.17%的场景增加、26.67%改善、14.17%持平。
- 两方法安全违反场景均为0；两个主要终点同时不劣比例40.83%，未达到联合价值门槛60%。MPC动作变化次数配对中位增加24次，说明后续还需考虑执行器动作代价。
- 分组结果显示蒸散低估组和浅根区组的亏缺代价较高，可作为T011风险监督的首要候选信息。分组结果只用于边界定位。
- 配对证据和正式结果JSON连续两次运行SHA-256一致；非确定性计算计时已与确定性科学结果分离。
- T011已完成`mpc`、`rule_fallback`和`safe_pause`三模式可信监督状态机。关键观测或执行链异常优先进入安全暂停，MPC不可用或高风险进入规则回退。
- 安全暂停恢复时必须先进入规则回退；规则回退连续满足恢复阈值、求解成功及最小驻留后才能返回MPC。高风险与恢复阈值分离形成滞回。
- 当前监督风险阈值为归一化暂定值，尚未经过开发异常场景标定，不得用于性能主张。单传感器漂移和模型残差不可分离性已写入设计边界。
- T013已建立12类确定性压力日程：正常、蒸散低估、根深突变、排水突变、传感偏置、传感漂移、缺测、尖峰、流量衰减、阀门卡滞、求解失败和反馈冲突。开发集每类4个，锁定评价集每类10个。
- 风险尺度仅使用48个开发场景的正常运行轨迹校准，共16128个时间步，其中有效灌水命令时间步1663个；锁定评价场景使用数为0。
- 冻结尺度：观测变化率0.005631至0.008233，模型残差EWMA 0.001584至0.002134，执行相对误差0.03855至0.05699；低、高端点对应开发正常特征的95%和99.5%分位数。
- 在开发正常特征中，三个风险分量达到0.7的时间步比例分别约1.13%、1.06%和0.96%。这不是闭环误回退率，T014必须单独评价正常场景代价。
- 异常清单、冻结监督配置和校准审计连续两次生成SHA-256一致。异常幅值是压力设置，不代表现场故障分布。
- T014已完成120个锁定异常场景、3种方法和360次闭环运行。正式分类为`PARTIAL_MECHANISM_VALUE_WITH_BOUNDARIES`，未通过预注册机制价值门槛。
- 可信监督相对基础MPC仅在求解失败1类异常改善，在反馈冲突、流量衰减、传感偏置、传感漂移、传感缺测和阀门卡滞6类异常上恶化；事件窗口亏缺配对中位差为0，95%区间[0, 0.001235]。
- 正常场景非MPC占比中位数4.46%，可信监督安全违反场景0个、基础MPC 1个。风险检测能触发，但现有安全暂停和规则回退不能稳定转化为水分控制收益。
- 当前监督框架可作为否定性机制证据和下一版设计起点，不能作为已验证优越算法。下一版必须使用新的开发异常集和新的独立锁定集。
- T014配对表与正式结果JSON连续两次运行SHA-256一致；测试20项通过。
- T012已完成最小管控平台软件在环：3个代表任务、1008个控制步、1008条命令与1008条反馈全部一一关联，另记录154条联锁状态和35次模式切换。
- 软件在环的6项账目约束全部通过，控制器与监督配置哈希绑定到每个任务；正式JSON和任务导出连续两次运行一致。
- T012只验证本地确定性审计语义，尚未覆盖网络延迟、消息乱序/重复、崩溃恢复、实时截止时间及实体PLC或泵阀协议。
- T016已完成估计驱动回退V2开发。新建96个开发场景和144个独立锁定场景，12类异常分别为开发集每类8个、锁定集每类12个；开发阶段锁定场景使用数为0。
- V2在观测不可信时拒绝观测校正，依靠内部模型、蒸散估计和可信执行反馈递推状态，并以估计下界驱动回退。只有执行器明确不可用时才安全暂停。
- 8组候选、基础MPC和V1共完成960次开发运行，冻结唯一可行候选V2C005：观测增益0.65、不确定度每步增长0.00025、上限0.006、回退启动阈值0.205。
- V2C005在V1此前恶化的6类异常上全部改善，相对基础MPC有1类中位恶化；正常非MPC占比中位数8.93%，安全违反0，灌水命令中位数78 mm。该结果属于开发选择，不是确认性证据。
- V2开发轨迹、候选汇总、冻结配置和正式JSON连续两次哈希一致；测试26项通过。

## 证据边界

- 当前全部结果属于模型仿真。
- 当前虚拟对象不是经红壤实测数据校准的数字孪生。
- 当前参数只用于检查研究链路和代码正确性，不支持吉安红壤适用性、节水率、增产率或肥料利用率结论。
- 烟雾试验用于发现错误，不作为正式论文结果。
- 基础 MPC 是否优于强规则必须由后续锁定场景、独立随机种子和统计分析决定。

## 数据与任务规则

- `data/csv/task_list.csv` 是任务状态的唯一事实来源。
- 原始外部数据放入 `data/raw/`，不得覆盖；清洗数据放入 `data/interim/`；冻结分析数据放入 `data/processed/`。
- 正式实验必须记录配置文件、随机种子、代码提交号和输出目录。
- 禁止根据锁定评价结果反复调整控制参数。

## 下一步

1. 启动T017，先冻结V2确认性评价契约，再首次运行144个新锁定场景。
2. 后续平台阶段再加入消息乱序、重复投递、超时和崩溃恢复测试。
3. 现场数据到位前，结论继续限定为仿真机制与软件链路可行性。

## 新窗口启动顺序

1. 只读本文件。
2. 读取 `data/csv/task_list.csv` 确认当前任务。
3. 查看 `git status` 和最近提交。
4. 读取当前任务涉及的配置与源码，避免无目的全仓库扫描。
